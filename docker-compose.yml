services:
  redis:
    image: redis/redis-stack-server:8.0
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis-data:/data
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M
    command: >
      redis-stack-server
      --save 60 1000
      --appendonly yes
      --maxmemory 1536mb
      --maxmemory-policy noeviction
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  agentmem:
    build: .
    ports:
      - "127.0.0.1:18800:18800"
    environment:
      - REDIS_URL=redis://redis:6379
      - AGENTMEM_API_KEY=${AGENTMEM_API_KEY:-}
      - AGENTMEM_LLM_API_KEY=${AGENTMEM_LLM_API_KEY:-}
      - AGENTMEM_LLM_BASE_URL=${AGENTMEM_LLM_BASE_URL:-}
      - AGENTMEM_LLM_PROVIDER=${AGENTMEM_LLM_PROVIDER:-}
      - AGENTMEM_LOG_LEVEL=${AGENTMEM_LOG_LEVEL:-INFO}
      - AGENTMEM_AMAC_THRESHOLD=${AGENTMEM_AMAC_THRESHOLD:-0.40}
      - AGENTMEM_BG_TASK_LIMIT=${AGENTMEM_BG_TASK_LIMIT:-50}
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 256M
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:18800/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  redis-data:
