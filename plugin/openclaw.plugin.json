{
  "id": "memos-local-openclaw-plugin",
  "name": "AgentMem — Local Agent Memory",
  "description": "AgentMem v1.0.0: A-MAC + SimpleMem + ToolMem + TIG + AWO + 6-tier persistent memory via Redis HNSW.",
  "version": "0.5.2",
  "kind": "lifecycle",
  "main": "./index.js",
  "configSchema": {
    "type": "object",
    "properties": {
      "baseUrl": {
        "type": "string",
        "description": "AgentMem service base URL",
        "default": "http://127.0.0.1:18800"
      },
      "memoryLimitNumber": {
        "type": "number",
        "description": "Max memories to recall per query",
        "default": 6
      }
    },
    "additionalProperties": false
  }
}
