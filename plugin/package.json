{
  "name": "agentmem-openclaw-plugin",
  "version": "0.3.0",
  "type": "module",
  "description": "AgentMem v0.9.2 — local persistent memory for OpenClaw via Redis HNSW + MiniLM.",
  "openclaw": {
    "extensions": ["./index.js"]
  }
}
